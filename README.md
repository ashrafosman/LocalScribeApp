# LocalScripeApp

Minimal PySide6 desktop app wrapper for realtime transcription.

## Setup

```bash
python -m venv .venv
source .venv/bin/activate
pip install -r requirements.txt
```

## Run

```bash
python main.py
```
